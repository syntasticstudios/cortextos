
## Cannaleo re-read WATCH CRITERION (analyst-confirmed) - 06:27Z
- task_1779854547122 OPEN until cannaleo lastSuccessAt ADVANCES past 2026-05-26T21:20:24Z (first completed full-sync post-BA-processing-fix) + band exits ALERT.
- Re-read instrument: syncRecencyAnalysis + checkProviderSyncStaleness + recentSyncLogErrors + providerCircuitBreaker:listAll (cannaleo).
- Trigger: BA deploy signal OR my ~10:06Z experiment tick (fallback). Report advance OR still-frozen (=fix ineffective datapoint).
- BA diagnostic now = lastProgressAt heartbeat (where in full-sync processing it stops). i-r /catalog probe deprioritized (CB API successes climbing = provider answers).

## SHARED dedup KEEP-precedence invariant (BA+me locked) - 06:55Z
- KEEP per (provider,productId,pharmacyId): (1) available=true>false (2) latest lastSeenAt (3) latest updatedAt (4) latest _creationTime; delete rest.
- BA inline collapse (task_1779863294817): selects survivor by precedence + PATCHES w/ incoming sync data + deletes N-1.
- MY migration (task_1779864325151): selects survivor by SAME precedence, keeps as-is (no incoming data), deletes rest; next sync refreshes. Same _id-survival → reconciled-identical either path.
- Both PRs cite the shared invariant. My migration built AFTER #1-fix unmasks offer-batch.
NOTE 07:30 UTC 05-27: daily-upstream-summary — upstream 0 pending, 0 applied 24h, 0 features awaiting. NO-NEWS digest → NOT sent as separate Telegram (consolidating into 11:00 briefing per lane-lock; avoid morning ping-spam). Briefing line: 'cortextOS upstream: 0 applied, 0 pending — framework in sync.' Fire recorded.
