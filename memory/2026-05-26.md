
## COMPLETED: KB venv worktree fix (10:15 CEST)
- PR #9 (cortextos) opened: fix(kb) getVenvPython worktree fallback
- Root cause: CTX_FRAMEWORK_ROOT points to worktree, venv only in main checkout
- Fix: existsSync check + worktree pattern match → fallback to main checkout venv
- Build: SUCCESS. Notified platform-director.

